# Base URL for the API. Leave empty to use the same origin (recommended in
# production behind Nginx, which proxies /api to the backend).
# For split-host setups, set e.g. VITE_API_BASE="https://api.example.com"
VITE_API_BASE=""
